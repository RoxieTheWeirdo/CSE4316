<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Camera not required -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/appicon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.fitbite"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <!-- Launcher -->
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Account setup -->
        <activity android:name=".CreateAccount" />
        <activity android:name=".activity_basics_sex" />
        <activity android:name=".activity_basics_birthday" />
        <activity android:name=".activity_basics_height" />
        <activity android:name=".activity_basics_weight" />
        <activity android:name=".activity_basics_exercise" />
        <activity android:name=".bodyReconfiguration" />

        <!-- Main app -->
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AutoMealPlan" />
        <activity android:name=".SearchFoodActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".EditFoodActivity" />

        <!-- Barcode -->
        <activity android:name=".BarcodeScanner"
            android:stateNotNeeded="true"/>
        <activity
            android:name=".CaptureAct"
            android:screenOrientation="portrait"
            android:theme="@style/zxingCameraTheme"/>
        <activity android:name=".BarcodeScannerAfter" />
        <activity android:name=".BarcodeMoreInfo" />
        <activity android:name=".PantryScanner" />

        <!-- Meals -->
        <activity android:name=".MealPlanActivity" />
        <activity android:name=".MealDetailActivity" />
        <activity android:name=".FoodDiaryActivity" />

        <!-- Settings -->
        <activity android:name=".SettingEditAccount" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SettingEditPersonal" />
        <activity android:name=".SettingEditApp" />
        <activity android:name=".SettingsOverview" />
        <activity android:name=".SettingChangeBirthday" />
        <activity android:name=".SettingChangeHeight" />
        <activity android:name=".SettingChangeWeight" />
        <activity android:name=".SettingChangeSex" />
        <activity android:name=".SettingChangeExercise" />
        <activity android:name=".SettingChangeAllergies" />
        <activity android:name=".WeightActivity" />

        <!-- New from main -->
        <activity android:name=".ExerciseActivity" />
        <activity android:name=".NotificationHistory" />

        <!-- FileProvider (keep yours) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files_paths" />
        </provider>

    </application>

</manifest>

